% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_maximin.R
\name{calc_maximin}
\alias{calc_maximin}
\title{Maximin multi-criterion approximate design on a finite candidate set}
\usage{
calc_maximin(
  u,
  f,
  criteria = c("D", "A"),
  cVec = NULL,
  solver = "CLARABEL",
  ...,
  drop_tol = 1e-08,
  bisect_tol = 1e-06,
  bisect_max_iter = 60L,
  min_eig = 1e-08
)
}
\arguments{
\item{u}{Candidate design points.}

\item{f}{Regression function returning a numeric vector (same convention as
\code{\link[=calc_Dopt]{calc_Dopt()}}).}

\item{criteria}{Character vector of criteria to combine: any of \code{"D"}, \code{"A"},
\code{"c"}. Duplicates are not allowed.}

\item{cVec}{Linear contrast for c-optimality; required if \code{"c"} is included.
Length must match \code{length(f(u[1]))}.}

\item{solver}{Solver name passed to \code{\link[CVXR:psolve]{CVXR::psolve()}}.}

\item{...}{Additional arguments passed to \code{\link[CVXR:psolve]{CVXR::psolve()}}.}

\item{drop_tol}{Weights below this (after normalization) are set to zero.}

\item{bisect_tol}{Stopping tolerance for the bisection on \eqn{t}.}

\item{bisect_max_iter}{Maximum bisection iterations.}

\item{min_eig}{Positive lower bound enforced on the minimum eigenvalue of the
information matrix (via \code{\link[CVXR:lambda_min]{CVXR::lambda_min()}}). This avoids numerically
singular designs that can satisfy loose \code{log_det} constraints but yield
meaningless efficiencies.}
}
\value{
A list of class \code{c("cvx_maximin_design", "cvx_design")} with
components including \code{design}, \code{weights}, \code{info_matrix}, \code{criteria},
\code{value} (the maximin efficiency \eqn{t^\star}{t*}), \code{efficiencies} (named
vector of efficiencies at the returned design), \code{references} (single-objective
values used for normalization), and solver diagnostics.
}
\description{
Computes a design that maximizes the minimum efficiency across several
classical criteria (D, A, c), each measured relative to the best value
achievable on the same candidate set under that criterion alone. This matches
the usual maximin-efficiency formulation used in multi-objective design
(similar in spirit to \code{compute_maximin_design} in group-testing code).
}
\details{
For a criterion with optimal reference value \eqn{v_j^\star}{v_j*} (from the
corresponding single-objective problem on \code{u}) and value \eqn{v_j(w)}
at design \eqn{w}, the efficiency is \eqn{e_j(w) = v_j^\star / v_j(w)} for
criteria minimized at the optimum (A, c), and
\eqn{e_D(w) = (\det M(w) / \det M_D^\star)^{1/p}}{D-efficiency in the usual
determinant sense} for D-optimality, where \eqn{p} is the number of
parameters.

The algorithm bisects \eqn{t \in (0,1]} and solves a convex feasibility
problem at each \eqn{t} (maximize \eqn{\min_j e_j(w) \ge t}{min_j e_j(w) >= t}),
then refines with a secondary objective among maximin-efficient designs.
}
