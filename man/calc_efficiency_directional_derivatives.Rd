% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_efficiency_directional_derivatives.R
\name{calc_efficiency_directional_derivatives}
\alias{calc_efficiency_directional_derivatives}
\title{Directional derivatives of D-, A-, and c-efficiencies (Kiefer--Wolfowitz kernel)}
\usage{
calc_efficiency_directional_derivatives(
  u,
  M,
  f,
  criteria,
  efficiencies,
  cVec = NULL,
  use_ginv = TRUE
)
}
\arguments{
\item{u}{Candidate design points.}

\item{M}{Information matrix at the design.}

\item{f}{Regression function.}

\item{criteria}{Character vector among \code{"D"}, \code{"A"}, \code{"c"}.}

\item{efficiencies}{Named numeric vector with elements \code{D}, \code{A}, and/or \code{c}
giving efficiencies at \code{M} (same normalization as \code{\link[=calc_maximin]{calc_maximin()}}).}

\item{cVec}{Contrast for \code{"c"}.}

\item{use_ginv}{Passed to \code{\link[=calc_directional_derivatives]{calc_directional_derivatives()}}.}
}
\value{
Named list with components \code{dD}, \code{dA}, and/or \code{dc} containing
efficiency derivatives on \code{u} (same names as KW list for use with
\code{\link[=calc_multi_directional_derivative]{calc_multi_directional_derivative()}}).
}
\description{
Transforms the usual loss-based directional derivatives from
\code{\link[=calc_directional_derivatives]{calc_directional_derivatives()}} into derivatives of \strong{efficiencies}
\eqn{e_D, e_A, e_c} with respect to the same one-point perturbation of the
design. These are the objects that enter subgradient conditions for
\strong{maximin efficiency} designs (maximize \eqn{\min_j e_j}), whereas raw
\eqn{d_D, d_A, d_c} are appropriate for \strong{linear compound} loss criteria.
}
\details{
Let \eqn{d_D^{\mathrm{kw}}}{dD_kw}, \eqn{d_A^{\mathrm{kw}}}{dA_kw},
\eqn{d_c^{\mathrm{kw}}}{dc_kw} denote the outputs of
\code{\link[=calc_directional_derivatives]{calc_directional_derivatives()}}. With \eqn{p} parameters,
\eqn{T = \mathrm{tr}(M^{-1})}{T = tr(M^-1)}, \eqn{V = c'M^{-1}c}, reference
values from single-objective optima on the same candidate set, and current
efficiencies \eqn{e_D, e_A, e_c}:
\deqn{\frac{\mathrm{d} e_D}{\mathrm{d}\xi} = \frac{e_D}{p} \, d_D^{\mathrm{kw}}, \quad
      \frac{\mathrm{d} e_A}{\mathrm{d}\xi} = -\frac{e_A}{T} \, d_A^{\mathrm{kw}}, \quad
      \frac{\mathrm{d} e_c}{\mathrm{d}\xi} = -\frac{e_c}{V} \, d_c^{\mathrm{kw}}.}
}
