% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimize_maximin_equiv_eta.R
\name{optimize_maximin_equiv_eta}
\alias{optimize_maximin_equiv_eta}
\title{Weights that minimize the worst-case combined derivative on a grid}
\usage{
optimize_maximin_equiv_eta(dd_list, criteria, step = 0.02)
}
\arguments{
\item{dd_list}{Output of \code{\link[=calc_efficiency_directional_derivatives]{calc_efficiency_directional_derivatives()}} (same
names \code{dD}, \code{dA}, \code{dc}).}

\item{criteria}{Character vector of criteria in the same order as used for
\code{dd_list} (e.g. \code{c("D", "A")}).}

\item{step}{Grid step on the simplex for \eqn{k = 3} (ignored for \eqn{k = 2}).}
}
\value{
A list with \code{eta} (named \code{d}, \code{a}, \code{c} style), \code{min_max} (the
minimized maximum), \code{combined} (vector on the grid), and \code{convex_combination}
(same as \code{combined}).
}
\description{
Given efficiency-based directional derivatives \eqn{de_j(u_i)} (from
\code{\link[=calc_efficiency_directional_derivatives]{calc_efficiency_directional_derivatives()}}) on a candidate grid \eqn{u_i}, a
necessary finite-dimensional check for a \strong{maximin efficiency} design is
that there exist convex weights \eqn{\eta} with \eqn{\sum_j \eta_j de_j(u_i)
\le 0}{sum eta de_j <= 0} for all \eqn{i}. Heuristic rules (binding
efficiencies, uniform) need not achieve this even when such an \eqn{\eta}
exists.
}
\details{
This function solves
\deqn{\min_{\eta \in \Delta} \max_i \sum_j \eta_j \, de_j(u_i)}
over the simplex \eqn{\Delta} (grid search for \eqn{k \le 3}{k <= 3}).
}
