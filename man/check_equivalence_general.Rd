% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_equivalence_general.R
\name{check_equivalence_general}
\alias{check_equivalence_general}
\title{Equivalence theorem check for general criteria (single or maximin)}
\usage{
check_equivalence_general(
  design_obj,
  f,
  u = NULL,
  criteria = NULL,
  cVec = NULL,
  eta = NULL,
  eta_rule = c("binding", "uniform", "optimize"),
  tstar = NULL,
  eta_tol = 1e-05,
  tol = 1e-06,
  use_ginv = TRUE,
  derivative = c("efficiency", "kw_loss")
)
}
\arguments{
\item{design_obj}{Output from \code{\link[=calc_Dopt]{calc_Dopt()}}, \code{\link[=calc_Aopt]{calc_Aopt()}}, \code{\link[=calc_copt]{calc_copt()}}, or
\code{\link[=calc_maximin]{calc_maximin()}}.}

\item{f}{Regression function (same as used for the design).}

\item{u}{Candidate design points; defaults to \code{design_obj$candidates}.}

\item{criteria}{Character vector of criteria to check. For single-objective
designs, defaults to \code{design_obj$criterion}. For maximin, defaults to
\code{design_obj$criteria}.}

\item{cVec}{Contrast for c-optimality; required when \code{"c"} is included and not
stored on \code{design_obj} (for maximin, \code{ref_values$c_vec} is used if present).}

\item{eta}{Optional named numeric vector of weights (names like \code{"d"}, \code{"a"},
\code{"c"}). If \code{NULL}, maximin uses \code{eta_rule}; single-criterion uses weight 1.}

\item{eta_rule}{For maximin when \code{eta} is \code{NULL}: \code{"binding"}, \code{"uniform"},
or \code{"optimize"} (see Details).}

\item{tstar}{Maximin level; for maximin designs defaults to \code{design_obj$value}.}

\item{eta_tol}{Tolerance for matching each efficiency to \code{tstar} when forming
automatic \code{eta} under \code{eta_rule = "binding"}.}

\item{tol}{Tolerance for nonpositivity and zero-on-support checks.}

\item{use_ginv}{Passed to \code{\link[=calc_directional_derivatives]{calc_directional_derivatives()}}.}

\item{derivative}{For \strong{maximin} designs only: \code{"efficiency"} (default)
uses \code{\link[=calc_efficiency_directional_derivatives]{calc_efficiency_directional_derivatives()}} so the combined curve
reflects derivatives of \eqn{e_D, e_A, e_c}; \code{"kw_loss"} uses raw
Kiefer--Wolfowitz derivatives of the underlying losses (diagnostic only for
maximin efficiency).}
}
\value{
An object of class \code{c("cvx_equivalence_general", "cvx_equivalence")}
with components including \code{directional_derivative} (the curve to plot:
single criterion or combined), \code{directional_derivatives} (named list per
criterion for maximin), \code{derivative} (\code{"efficiency"} or \code{"kw_loss"} for
maximin), \code{eta} (weights, maximin only), \code{eta_optimization} (when
\code{eta_rule = "optimize"}, output of \code{\link[=optimize_maximin_equiv_eta]{optimize_maximin_equiv_eta()}}),
\code{max_violation}, \code{all_nonpositive}, \code{support_equal_zero}, and \code{mode}
(\code{"single"} or \code{"maximin"}).
}
\description{
For a \strong{single} criterion (D, A, or c), checks that the directional derivative
is nonpositive on the candidate set and zero on the support of the optimal
design (standard equivalence theorem).
}
\details{
For a \strong{maximin} design (\code{\link[=calc_maximin]{calc_maximin()}}), by default (\code{derivative = "efficiency"}) the function uses \code{\link[=calc_efficiency_directional_derivatives]{calc_efficiency_directional_derivatives()}},
i.e. derivatives of the \strong{efficiencies} \eqn{e_D, e_A, e_c} in the same
Kiefer--Wolfowitz sense, then forms \eqn{\sum_j \eta_j \, \mathrm{d} e_j /
\mathrm{d}\xi}{sum eta * de_j}. That matches the subgradient of the maximin
efficiency objective far better than weighting raw loss derivatives.

Use \code{derivative = "kw_loss"} only if you want the old behaviour (derivatives
of \eqn{-\log\det M}{-log det M}, \eqn{\mathrm{tr}(M^{-1})}{tr(M^-1)}, etc.),
e.g. for comparison to linear compound designs.

Weights \eqn{\eta} (see \code{eta_rule}):
\itemize{
\item \code{"binding"}: uniform over criteria whose \strong{efficiency} equals \eqn{t^\star}
within \code{eta_tol}, zero elsewhere; if none match, uniform over all.
\item \code{"uniform"}: \eqn{\eta_j = 1/k} for all \eqn{k} criteria (often clearer for
comparing contributions in plots).
\item \code{"optimize"}: choose \eqn{\eta} on the simplex to minimize
\eqn{\max_u \sum_j \eta_j \, de_j(u)}{max sum eta de_j} over the candidate
grid (\code{\link[=optimize_maximin_equiv_eta]{optimize_maximin_equiv_eta()}}); best finite check when peaks remain
slightly above zero with other rules.
}
}
\section{Calling the function}{

If you see \verb{unused argument (derivative = ...)} while \code{args(cvxDesign::check_equivalence_general)}
lists \code{derivative}, another object is masking the package function (e.g. an old
copy from \code{source()} or another session). Use the namespace-qualified call:
\code{cvxDesign::check_equivalence_general(...)} or run \code{find("check_equivalence_general")}
and remove the masking binding.
}

\examples{
\donttest{
if (requireNamespace("CVXR", quietly = TRUE)) {
  ## Quadratic regression: f(x) = (1, x, x^2)'
  f <- function(x) c(1, x, x^2)
  u <- seq(-1, 1, length.out = 51)

  ## --- Single-objective D-optimal design ---
  des <- calc_Dopt(u, f)
  eq <- check_equivalence_general(des, f)
  eq$max_violation
  eq$all_nonpositive
  eq$support_equal_zero
  plot_equivalence(eq)

  ## Same model via compute_design_SO (MATLAB-style scalar del)
  des2 <- compute_design_SO(u, f, criterion = "D")
  eq2 <- check_equivalence_general(des2, f, criteria = "D")

  ## --- Maximin (D + A): use efficiency derivatives (default) ---
  mm <- calc_maximin(u, f, criteria = c("D", "A"))
  eqm <- check_equivalence_general(mm, f, eta_rule = "uniform")
  plot_multi_equivalence(
    u,
    eqm$directional_derivatives,
    eqm$directional_derivative,
    criteria = mm$criteria
  )
}
}

}
\seealso{
\code{\link[=check_equivalence]{check_equivalence()}} for the original single-object API,
\code{\link[=plot_equivalence]{plot_equivalence()}}, \code{\link[=plot_multi_equivalence]{plot_multi_equivalence()}}.
}
